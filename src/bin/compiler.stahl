(module main)
(export main replicate)
(import std (Byte List Nat Vect S Z nil))

(defn replicate (T *) (x T) (n Nat) -> (Vect T n)
  (case n
    (S n2) (cons x (replicate T x n2))
	Z      nil))

(defn main (args (List String)) -> Byte
  (println "Hello, world!")
  0)
