(module parser)
(export parse-string)
(import std (Either String))
(import std/fs (Path))
(import std/sexpr (Sexpr))
(import std/util (TODO))

(type parse-string Path String -> (Either Error Sexpr))
(defn parse-string (path str)
  TODO)
