(module geom:math
  * +)

(def * (pi ((_ Fixnum) (_ Fixnum)) Fixnum)
  (fn (x y)
    (rec Nat
      (const Nat)
      zero
      (fn (z) (+ x z))
      y)))

(def + (pi ((_ Fixnum) (_ Fixnum)) Fixnum)
  (fn (x y)
    (rec Nat
      (const Nat)
      x
      succ
      y)))
